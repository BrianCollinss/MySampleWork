[project]
name = "crm-revenue-intelligence"
version = "0.1.0"
description = "End-to-end CRM and pipeline analytics project with medallion architecture."
requires-python = ">=3.11"
dependencies = [
  "duckdb>=1.1.0",
  "matplotlib>=3.8.0",
  "pandas>=2.2.0",
  "pyarrow>=16.0.0",
  "pydantic>=2.7.0",
  "scikit-learn>=1.5.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
