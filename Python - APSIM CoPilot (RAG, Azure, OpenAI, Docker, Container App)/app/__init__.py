"""Top-level package for APSIM Copilot.

This package groups the Streamlit entrypoint, service layer, and helper models
used by the demo application. Keeping a single import root makes the app easier
to run both locally and inside containerised environments.
"""
