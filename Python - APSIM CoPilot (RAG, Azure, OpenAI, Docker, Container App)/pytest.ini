# Pytest configuration for APSIM Copilot.
# Restrict discovery to the dedicated tests folder so helper scripts such as the
# smoke test are not collected as unit tests.
[pytest]
python_files = tests/test_*.py
addopts = -p no:cacheprovider
